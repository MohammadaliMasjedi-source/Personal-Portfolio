# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Logs / misc
*.log
node_modules/
.cache/

# Private working notes and draft content — never publish
_private/
content/

# Machine-local launcher / local tooling (not part of the public site)
MC-Dashboard.bat
.tooling/

# Mission-Control project record — machine-local phase notes with internal wording.
# Was tracked here once (removed in the 2026-07 history rewrite); guarded so a fresh
# clone or sweep can't re-add it. Never commit this folder to the public repo.
.mc/

# local Claude tooling — never in the public-standard repo
.claude/

# SEO staging drafts — review-only, never deployed until Mo flips them live
_staging-seo/

# findability/name pack — DRAFT + supervisor-GATED (unpublished paper titles, internal paths).
# Canonical home is the PRIVATE repo open-source-contributions/drafts/name-pack/.
# Leaked to this public repo once (commit 2ace8b3, 2026-07-19) then removed; guarded here so a
# future sweep can't re-add it. Never commit this folder to the public repo.
findability/
