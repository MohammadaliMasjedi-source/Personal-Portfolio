# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*~

# Logs / misc
*.log
node_modules/
.cache/

# Private brand kit (Wikipedia drafts, facts, strategy) — never publish
_private/

# research/working content (holds 'do-not-publish' flags + unverified claims) — never publish
content/
